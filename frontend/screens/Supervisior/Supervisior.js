import React from "react";
import { View, StyleSheet, Text } from "react-native";

const Supervisior = () => {
  return (
    <View>
      <Text>Supervisior</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Supervisior;
